# generated by pebble gcc (minimal C subset)
__gcc_return = 0
seed = 17
a = seed * 3 + 5
b = a * a - 9
c = b / 4 + a * 2
mix1 = c * 31 + 7
mix2 = mix1 * 17 + b
mix3 = mix2 * 13 + a
checksum = mix3 + seed + c
score_math = a + b + c
score_mix = mix1 + mix2 + mix3
final_score = score_math + score_mix + checksum
print a
print b
print c
print checksum
print final_score
__gcc_return = final_score
