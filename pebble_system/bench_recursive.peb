def fib(n):
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)

def repeat_fib(count, value):
    total = 0
    i = 0
    while i < count:
        total = total + fib(value)
        i = i + 1
    return total

print repeat_fib(12, 16)
