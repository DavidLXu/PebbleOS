if ARGC == 0:
    print "usage: rm FILE [FILE ...]"
else:
    i = 0
    while i < ARGC:
        name = argv(i)
        try:
            delete_file(name)
            print "deleted " + name
        except err:
            print "rm: cannot remove '" + name + "': " + err
        i = i + 1
